import { useOutletContext } from "react-router-dom";
import BannerSection from "../../components/home/BannerSection"; import FaqSection from "../../components/home/FaqSection"; import HeroSection from "../../components/home/HeroSection"; import HowItWorks from "../../components/home/HowItWorks"; import PopularServices from "../../components/home/PopularServices"; import Testimonials from "../../components/home/Testimonials";
const fallbacks = { hero: <HeroSection />, featuredServices: <PopularServices /> };
const Home = () => { const { data, loading, error } = useOutletContext(); const visibility = data?.sectionVisibility || {}; const sections = { hero: <HeroSection hero={data?.hero} />, banners: <BannerSection banners={data?.banners} />, featuredServices: <PopularServices services={data ? data.featuredServices : undefined} />, howItWorks: <HowItWorks />, testimonials: <Testimonials items={data?.testimonials} />, faqs: <FaqSection items={data?.faqs} /> }; const order = data?.sectionOrder?.length ? data.sectionOrder : ["hero", "featuredServices"]; return <>{loading && <div className="mx-auto max-w-7xl px-4 py-3 text-sm text-slate-500">Loading the latest content…</div>}{error && <div className="mx-auto max-w-7xl px-4 py-3 text-sm text-amber-700">{error} Showing the standard homepage.</div>}{order.map((key) => (data && visibility[key] === false ? null : <div key={key}>{sections[key] || fallbacks[key]}</div>))}</>; };
export default Home;
