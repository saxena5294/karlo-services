import { Link } from "react-router-dom";
const BannerSection = ({ banners = [] }) => banners.length ? <section className="mx-auto grid max-w-7xl gap-5 px-4 py-10 sm:px-6 lg:px-8">{banners.map((banner) => <article key={banner._id} className="overflow-hidden rounded-3xl bg-blue-700 text-white shadow-lg"><div className="grid items-center md:grid-cols-2">{banner.image?.url && <img src={banner.image.url} alt="" className="h-56 w-full object-cover md:h-full"/>}<div className="p-7 sm:p-10"><h2 className="text-2xl font-bold sm:text-3xl">{banner.title}</h2>{banner.description && <p className="mt-3 text-blue-100">{banner.description}</p>}{banner.buttonText && banner.buttonLink && <Link to={banner.buttonLink} className="mt-6 inline-flex rounded-xl bg-white px-5 py-3 font-semibold text-blue-700">{banner.buttonText}</Link>}</div></div></article>)}</section> : null;
export default BannerSection;
