export const FULFILLMENT_TYPES = Object.freeze({
  INTERNAL: "internal",
  PARTNER: "partner",
  HYBRID: "hybrid",
});

export const FULFILLMENT_TYPE_VALUES = Object.freeze(Object.values(FULFILLMENT_TYPES));

export const ASSIGNMENT_TYPES = Object.freeze({
  EXPERT: "expert",
  PARTNER: "partner",
});

export const ASSIGNMENT_TYPE_VALUES = Object.freeze(Object.values(ASSIGNMENT_TYPES));

